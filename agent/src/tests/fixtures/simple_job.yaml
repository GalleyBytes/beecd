apiVersion: batch/v1
kind: Job
metadata:
  name: test-job
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: job-container
        image: busybox:latest
        command: ["sh", "-c", "echo completed"]
      restartPolicy: Never
  backoffLimit: 3
