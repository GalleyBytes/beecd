apiVersion: batch/v1
kind: Job
metadata:
  name: post-deploy-job
  namespace: default
  annotations:
    beecd/post-weight: "1"
spec:
  template:
    spec:
      containers:
      - name: job
        image: job:1.0
        command: ["sh", "-c", "echo done"]
      restartPolicy: Never
  backoffLimit: 1
